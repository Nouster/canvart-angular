import { ButtonEffectsDirective } from './button-effects.directive';

describe('ButtonEffectsDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonEffectsDirective();
    expect(directive).toBeTruthy();
  });
});
